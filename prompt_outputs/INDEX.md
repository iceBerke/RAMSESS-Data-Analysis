# Index of prompt outputs

**The convention this directory follows is defined in `RULES.md`, under "Saving
prompt outputs".** That section is the authority; this file only lists what is
here.

One line per entry, in order. Number, slug, phases present, date, and what the
task did or found. Read-only work is listed too: an audit that found nothing is
a result, and the record should show it was run.

Append a line for every new entry. Never rewrite an existing one — if a task is
re-run, the new entry says which number it supersedes.

| NNN | Slug | Phases | Date | What it did or found |
|-----|------|--------|------|----------------------|
| 001 | rules-of-engagement-v2 | A, B | 2026-08-30 | Introduced this directory and its conventions; added the prompt-output, commit-gate and phase-failure sections to `RULES.md`, moved the commit out of PHASE B, and corrected a wrong attribution of line-ending conversion to `.gitattributes` — the mechanism is `core.autocrlf=true` at system scope. |
| 002 | record-gap-audit | audit | 2026-09-01 | Found that 001's PHASE B file was a subset of its chat report, missing the deviation disclosure, the commit-message validation and "matters next"; reproduced all three verbatim rather than amending 001, which the append-only boundary forbids. Also records that `prompt_outputs/README.md` was created and deleted in one session, leaving no trace in git. |
| 003 | rules-corrections | B | 2026-09-01 | Applied six corrections to `RULES.md`: the stale `git show` reference, the append-only boundary, the `Record:` line linking a commit to its entry, the file-is-a-superset-of-chat requirement, the ban on proposing a commit message inside a PHASE B report, and the deletion of `README.md`. Written as its own entry because 001 was frozen and 002 is read-only. Then extended by C7, allowing one `Record:` line per entry when a single commit covers several. |
| 004 | presentation-band-heights | A | 2026-09-01 | Verified that `data/derived/irradiation_sara/bands.csv` already holds absolute, baseline-corrected, un-normalised `height` at the located maximum, and that it is current — all 46 raw SHA-256 hashes still match and the recorded baseline parameters equal `baseline.json`. So deliverable 2 is extraction, not measurement. Two findings: `data/raw/irradiation_sara/` contains no acquisition metadata whatsoever, so ech2-vs-ech4 comparability is UNKNOWN FROM THE DATA ALONE; and of the 16 shifts of interest only 6 are configured bands, 5 sit in an acquired window with nothing configured, and ~3600–3700 free O–H is outside the acquired range entirely. Also records that `print_band_summary` does print absolute heights across samples, but for the reference band only, to stdout. |
| 005 | annotated-overlays | A | 2026-09-01 | Scoped annotated copies of the sample overlays. Established that plain `plot` reads no configuration file at all, so annotation adds a real dependency on `bands.json`, whose loader raises when absent and needs `common_window_ranges(spectra)` — one call `plot` does not make but has the input for. Confirmed `plot` has no dependency on `data/derived/`, so annotation must mark configured centres, not the located maxima in gitignored `bands.csv`. Measured label geometry: at 5.057 cm-1 per drawn point, horizontal labels collide on 4 of 7 adjacent pairs, worst `glycine_2975`/`glycine_3012` at 37 cm-1 = 7.3 pt against 53.3 pt labels, and headroom above the traces is only ~4.6% of panel height. Two decisions left open: ech2's low window puts all four glycine bands under 8.4% of panel height against si_522 at 100%, and whether an absent `bands.json` should be an error. |
| 006 | annotated-overlays | B | 2026-09-01 | Added `--annotate` to `plot`, writing `{sample}_overlay_annotated{_log}.png` and its `_baseline` variant. Labels are rotated upright and anchored to the top of the panel with a rule down to the axis, so placement no longer depends on peak height; rows are stacked using extents measured from the renderer, never a hardcoded spacing. On the real data the low panel needs one row and the high panel two, with `glycine_3012` dropped behind `glycine_2975` and `glycine_3146` returning to the top row. Suite 318 to 327 passed, 0 warnings; the six reference overlays verified byte-identical before and after. Proved the new byte-difference test by making the annotation a no-op and pasting the four failures. Corrects entry 005 B3: there were seven literal 3-tuples in `test_output_filenames.py`, not six. One finding left for the experimenter: on `ech4 --annotate --baseline --logy` the `loc="best"` legend moves into the reserved label band and overlaps `si_522`. |
| 007 | annotation-legibility | A | 2026-09-01 | Measured the cost of three defects the experimenter found in 006's figures: the rule running through its own label, labels too small and faint for a projector, and the legend moving into the reserved band. Found that raising the font to 11 or 12 adds no label row anywhere — the low window holds one row up to size 14 and only flips at 15 — so the real cost is the reserved band, which grows from 37.9% to 58.1% of the high panel between sizes 8 and 12. Confirmed `loc="best"` does honour `bbox_to_anchor`, fixing the label collision 1-of-16 to 0-of-16, but measured that it pushes the legend onto the traces in four further panels, all log-scaled, taking trace-touches from 7 to 30 — left as the one open decision. Established that the rule fix needs the drawing moved into the placement loop, and that none of the three fixes can alter the six reference overlays provided the legend fix goes inside the annotation helper rather than reordering `panel.legend`. |
| 008 | annotation-legibility | B | 2026-09-01 | Fixed all three: rules now stop below the foot of their own label, labels are the configured centre at font 12 in near-black with the rule left grey, and the legend is confined below the reserved band by `set_bbox_to_anchor` inside the annotation helper. Shortening the label from band name to centre more than paid for the larger font — the high panel's reserved band went from 37.9% at font 8 with names to 24.9% at font 12 with centres, and label-trace clearance improved too. Suite 327 to 342 passed, 0 warnings; the six reference overlays verified unmodified. Legend/label overlaps 1-of-16 to 0-of-16, and the legend-over-trace cost came in at 3 panels and 18 trace-touches against entry 007's predicted 5 and 30, because the smaller band left `loc="best"` more room. Records that the F3 test as literally specified PASSED against the reverted fix — whether a legend in the band hits a label is horizontal luck — so it gained a second assertion that the legend must stay below the lowest label's foot, which does fail. |
